// next.config.js
/**
//  @type {import('next').NextConfig} */
// const nextConfig = {
//     images: {
//       domains: ['localhost'],  // Permite imágenes de localhost
//     },
//   };

//   export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['node-flight956-backend.onrender.com'],  // Permite imágenes de localhost
  },
};

export default nextConfig;
